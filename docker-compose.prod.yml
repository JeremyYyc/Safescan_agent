services:
  db: {restart: unless-stopped}
  redis: {restart: unless-stopped}
  minio: {restart: unless-stopped}
  identity-access-service: {restart: unless-stopped}
  property-leasing-service: {restart: unless-stopped}
  maintenance-service: {restart: unless-stopped}
  inspection-report-service: {restart: unless-stopped}
  inspection-report-worker: {restart: unless-stopped}
  staff-portal-api: {restart: unless-stopped}
  tenant-portal-api: {restart: unless-stopped}
  staff-web: {restart: unless-stopped}
  tenant-web: {restart: unless-stopped}
  gateway: {restart: unless-stopped}
