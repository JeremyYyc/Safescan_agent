services:
  minio:
    ports:
      - "9000:9000"
      - "9001:9001"
  db:
    ports:
      - "5433:5432"

  backend:
    ports:
      - "8000:8000"

  frontend:
    ports:
      - "5173:80"

  gateway:
    profiles:
      - disabled
