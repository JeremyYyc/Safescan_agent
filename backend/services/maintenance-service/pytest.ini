[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    unit: deterministic unit test without external services
    api_contract: FastAPI/OpenAPI producer contract test
    mocked_dependency: dependency adapter test with explicit HTTP mock
    migration: migration declaration test
    postgres_integration: real PostgreSQL integration or constraint test
    concurrency: real PostgreSQL concurrency test
