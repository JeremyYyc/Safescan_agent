[pytest]
markers =
    unit: isolated tests that do not require external services
    postgres_integration: tests that require a migrated disposable PostgreSQL database
    concurrency: PostgreSQL transaction and locking race tests
