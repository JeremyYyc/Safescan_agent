# 架构决策记录（ADR）

ADR 记录已经接受、会影响多个模块且不应在普通代码修改中悄悄改变的决定。

- [ADR-0001：以数据所有权拆分后端微服务](ADR-0001-service-boundaries.md)

新增 ADR 使用 `ADR-NNNN-short-title.md`，包含状态、背景、决策、后果和替代方案。已接受的
ADR 不覆盖修改；发生方向变化时新增 ADR 并标记旧记录被替代。
